services:
  web:
    build: .
    container_name: cesarbritez-web
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:80"
    networks:
      - proxy

networks:
  proxy:
    external: true
