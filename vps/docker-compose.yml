name: cesarbritez

services:

  # ── Sitio web estático ────────────────────────────────────────
  web:
    build: ./web
    container_name: cesarbritez-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:${WEB_PORT:-8080}:80"
    volumes:
      # Solo las imágenes de productos del CRM, en solo lectura (NO el resto de uploads)
      - /var/lib/docker-data/aberturas/uploads/productos:/usr/share/nginx/html/uploads/productos:ro

  # ── Sitio web — API del catálogo online (solo lectura) ───────
  # Lee únicamente la vista catalogo_web con el rol web_catalogo. Sin puertos publicados:
  # nginx del contenedor web la alcanza por nombre (cesarbritez-api) en esta red.
  web-api:
    build: ./web/api
    container_name: cesarbritez-api
    restart: unless-stopped
    environment:
      DATABASE_URL: postgres://web_catalogo:${WEB_CATALOGO_DB_PASS}@aberturas-db:5432/postgres
      TZ: America/Argentina/Buenos_Aires
    depends_on:
      aberturas-db:
        condition: service_healthy

  # ── Aberturas CRM — Base de datos ────────────────────────────
  aberturas-db:
    image: postgres:15-alpine
    container_name: aberturas-db
    restart: unless-stopped
    environment:
      POSTGRES_DB:       postgres
      POSTGRES_USER:     postgres
      POSTGRES_PASSWORD: ${ABERTURAS_DB_PASS}
    volumes:
      - /var/lib/docker-data/aberturas/db:/var/lib/postgresql/data
      - ./aberturas/docker/initdb:/docker-entrypoint-initdb.d:ro
      - ./aberturas/supabase/migrations:/migrations:ro
    ports:
      - "127.0.0.1:${ABERTURAS_DB_PORT:-5434}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 10

  # ── Aberturas CRM — App ───────────────────────────────────────
  aberturas-app:
    build: ./aberturas
    container_name: aberturas-app
    restart: unless-stopped
    ports:
      - "127.0.0.1:${ABERTURAS_PORT:-3000}:3000"
    environment:
      DATABASE_URL: postgres://postgres:${ABERTURAS_DB_PASS}@aberturas-db:5432/postgres
      JWT_SECRET:   ${ABERTURAS_JWT_SECRET}
      APP_URL:      ${ABERTURAS_URL}
      NODE_ENV:     production
    volumes:
      - /var/lib/docker-data/aberturas/uploads:/app/uploads
    depends_on:
      aberturas-db:
        condition: service_healthy
